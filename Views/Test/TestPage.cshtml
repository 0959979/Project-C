@model zorgapp.ViewModels.TestListViewModel
<h2>Test Page</h2>

<form asp-action="StartTest" method="post">
    Patient Username:<br>
    <select name="TestId">
        @foreach (Tuple<string,string> t in Model.tuples)
        {
            <option value="@t.Item1">@t.Item2</option>
        }
    </select><br>
    <input type="submit" value="Submit" id="submit">
</form>